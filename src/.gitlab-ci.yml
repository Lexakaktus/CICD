stages:
  - build

build:
  stage: build
  script:
    - cd src/cat
    - make s21_cat
    - cd ../grep/
    - make s21_grep

  artifacts:
    paths:
      - src/cat/s21_cat
      - src/cat/s21_grep
    expire_in: 30 days
  only:
    - develop
